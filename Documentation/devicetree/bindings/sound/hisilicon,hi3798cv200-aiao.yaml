# SPDX-License-Identifier: (GPL-2.0-only OR BSD-2-Clause)
%YAML 1.2
---
$id: http://devicetree.org/schemas/sound/hisilicon,hi3798cv200-aiao.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#

title: HiSilicon Hi3798CV200 AIAO controller

maintainers:
  - linux-sound@vger.kernel.org

description:
  The Audio Input/Audio Output controller contains serial audio interfaces and
  native circular DMA buffers. The binding exposes the TX0 I2S interface as a
  standard ASoC DAI.

properties:
  compatible:
    const: hisilicon,hi3798cv200-aiao

  reg:
    maxItems: 1

  interrupts:
    maxItems: 1

  clocks:
    items:
      - description: AIAO working clock
      - description: Audio master PLL clock

  clock-names:
    items:
      - const: bus
      - const: mclk

  resets:
    maxItems: 1

  "#sound-dai-cells":
    const: 0

required:
  - compatible
  - reg
  - interrupts
  - clocks
  - clock-names
  - resets
  - "#sound-dai-cells"

allOf:
  - $ref: dai-common.yaml#

unevaluatedProperties: false

examples:
  - |
    #include <dt-bindings/clock/histb-clock.h>
    #include <dt-bindings/interrupt-controller/arm-gic.h>

    audio-controller@f8cd0000 {
        compatible = "hisilicon,hi3798cv200-aiao";
        reg = <0xf8cd0000 0x10000>;
        interrupts = <GIC_SPI 84 IRQ_TYPE_LEVEL_HIGH>;
        clocks = <&crg HISTB_AIAO_CLK>, <&crg HISTB_AIAO_MCLK>;
        clock-names = "bus", "mclk";
        resets = <&crg 0x118 4>;
        #sound-dai-cells = <0>;
    };
...
